---
// Kaggle — icono provisional local.
// Kaggle NO existe en svgl.app (verificado contra https://api.svgl.app/).
// TODO: reemplazar por el SVG oficial de marca si Kaggle publica uno
// o cuando aparezca en svgl.app.
interface Props {
  size?: number;
}
const { size = 16 } = Astro.props;
---

<svg
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="currentColor"
  aria-hidden="true"
  focusable="false"
>
  <path
    d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2Zm4.6 12.8-2.5-3.1-1.1 1.2v2.1h-2.5V8.1H13v3.2l3.3-3.2h3l-3.9 3.9 4.1 5.1h-2.9Z"
  ></path>
</svg>
